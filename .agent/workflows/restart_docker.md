---
description: Restart the Docker environment (Down, Build, Up)
---

Run the docker down, build, and up script to refresh the environment.

// turbo
```bash
./scripts/docker/docker_down_build_up.sh
```
